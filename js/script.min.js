document.addEventListener("DOMContentLoaded", () => {
  let s = document.getElementById("js-header-menu-button"),
    e = document.getElementById("js-drawer"),
    i = document.getElementById("js-drawer-bg"),
    t = document.getElementById("js-main-menu"),
    d = void window.scrollY,
    n = () => 1100 <= window.innerWidth;
  let a = () => {
      n()
        ? c()
        : (s.classList.add("is-visible"), s.classList.remove("is-hidden"));
    },
    c = () => {
      var e;
      t &&
        ((e = t.getBoundingClientRect().bottom),
        n() && 0 < e
          ? (s.classList.add("is-hidden"), s.classList.remove("is-visible"))
          : (s.classList.add("is-visible"), s.classList.remove("is-hidden")));
    };
  s.addEventListener("click", () => {
    e.classList.contains("is-active")
      ? (e.classList.remove("is-active"),
        s.classList.remove("is-checked"),
        i.classList.remove("is-active"))
      : (e.classList.add("is-active"),
        s.classList.add("is-checked"),
        i.classList.add("is-active"));
  }),
    i.addEventListener("click", () => {
      header.classList.remove("is-active"),
        s.classList.remove("is-checked"),
        i.classList.remove("is-active");
    }),
    window.addEventListener("scroll", () => {
      var e = window.scrollY;
      0 !== e ||
        n() ||
        (s.classList.add("is-visible"), s.classList.remove("is-hidden")),
        c(),
        (d = e);
    }),
    window.addEventListener("resize", () => {
      a();
    }),
    a();
}),
  (() => {
    let s = document.querySelector('meta[name="viewport"]');
    function e() {
      var e =
        360 < window.outerWidth
          ? "width=device-width,initial-scale=1"
          : "width=360";
      s.getAttribute("content") !== e && s.setAttribute("content", e);
    }
    addEventListener("resize", e, !1), e();
  })();
